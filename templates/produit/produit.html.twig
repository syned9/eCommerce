{% extends 'base.html.twig' %}

{% block title %}Hello ProduitController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {# <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/dinys/symfony/projet/eCommerce/src/Controller/ProduitController.php'|file_link(0) }}">src/Controller/ProduitController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/dinys/symfony/projet/eCommerce/templates/produit/index.html.twig'|file_link(0) }}">templates/produit/index.html.twig</a></code></li>
    </ul>
</div> #}
    <form action="{{ path('panier_add', {id: produit.id}) }}" method="post">
        <input type="number" name="quantite" max="{{ produit.quantite }}" min="1" value="1" placeholder="1">
        <button type="submite" class="btn btn-succes float-right">Ajouter</button>
    </form>
        {# {{ form(form_panier)}} #}
        {# <input type="number" name="quantite" max="{{ produit.quantite }}" min="1" value="1" placeholder="1">
        <button onclick="window.location.href = '{{ path('panier_add', {'id': produit.id }) }}';" type="submite" class="btn btn-succes float-right">Ajouter</button> #}
        {# <a href="{{ path('panier_add', {'id': produit.id }) }}" class="btn btn-succes float-right">
            <i class="fas fa-shopping-cart mr-1"></i>
            ajouter
        </a> #}
      <div class="card-body">
		<div class="row">
				{% for produit in produits %}
				<div class="col-6 col-md-4">
                        <a href="{{ path('produits', {id: produit.id}) }}">
						<div class="card" style="width: 19rem;">
							<img class="card-img-top" src="{{ asset ('/uploads/' ~ produit.UrlImage) }} " alt="tkt l'image arrive"
							class="card-img-top" style ="width:300px; height: 200px; margin-left: 1px; display:inline-block">
                            </a>
							<div class="card-body">
								<span class="font-weight-bold">{{ produit.nom }}
								</span>
								<p class="card-text text-truncate">Prix :
									{{ produit.prix }}
									€</p>
								  <a href="{{ path('produits', {id: produit.id}) }}" class="btn btn-primary">voir</a>
							</div>
						</div>
					</div>
			{% endfor %}
		</div>
	</div>


	{% block popup %}{% endblock %}


{% endblock %}
